export const MOBILE_SIZE = "768px";
export const MOBILE_QUERY = `(max-width: ${MOBILE_SIZE})`;

export const TABLET_SIZE = "1920px";
export const TABLET_QUERY = `(max-width: ${TABLET_SIZE})`;

export const DESKTOP_QUERY = `(min-width: ${TABLET_SIZE})`;
